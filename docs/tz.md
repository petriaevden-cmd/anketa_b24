# Техническое задание — Анкета (МКЦ + МП)

> **Статус:** актуальный документ.

---

## Проект

Локальное приложение Bitrix24 для заполнения анкеты клиента. Используется двумя ролями:
- **МКЦ** (менеджер колл-центра) — первичный сбор данных по телефону
- **МП** (менеджер продаж) — уточнение данных и запись клиента на встречу

Открывается как вкладка (таб) в карточке лида CRM. Интерфейс — iframe поверх Bitrix24 REST API и BX24 JS SDK. Backend — PHP.

---

## Утверждённый технологический стек

| Направление | Решение | Примечание |
|---|---|---|
| Frontend styles | **Tailwind CSS 4** | CDN-сборка (`@tailwindcss/browser@4` via jsdelivr), utility-first |
| UI components | **Flowbite 2** | Готовые компоненты поверх Tailwind |
| CRM / portal | **Bitrix24 REST API + BX24 JS SDK** | Локальное приложение, `CRM_LEAD_DETAIL_TAB` |
| Backend | **PHP** | `config.php`, `install.php`, `proxy.php`, `uninstall.php` |

**Правило:** стек зафиксирован. При каждой новой итерации UI не проектируется заново — интерфейс развивается внутри одного утверждённого стека и набора компонентов.

---

## Требования к frontend

### Общие правила

- Любой новый экран или переработка строится на **Tailwind CSS 4**.
- Стандартные UI-элементы (кнопки, формы, карточки, вкладки, алерты, спиннеры) берутся из **Flowbite**.
- Самописные CSS-файлы (`tokens.css`, `style.css`) **не подключаются** в `index.php` — помечены как устаревшие, оставлены только в git-истории.
- Кастомные `<style>` допустимы только точечно для Bitrix24 iframe/embed-особенностей (reset отступов, кастомный скроллбар).
- Новый дизайн **не проектируется с нуля** при каждой итерации.

### CDN-подключение

```html
<!-- Tailwind CSS v4 CDN -->
<script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

<!-- Flowbite 2 CSS -->
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/flowbite/2.3.0/flowbite.min.css" />

<!-- Flowbite 2 JS (в конце body) -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/flowbite/2.3.0/flowbite.min.js"></script>
```

### Layout

Основной экран — двухколоночный:
- **Левая колонка (55%)** — форма анкеты по блокам с прогресс-баром.
- **Правая колонка (45%)** — расписание МП + вкладка «Фишки» для будущих инструментов.

### Шапка приложения

- Название приложения без указания роли (не «Анкета КЦ» — приложение используется и МКЦ, и МП).
- В правом верхнем углу — **имя текущего пользователя** Bitrix24 (полученное через `BX24.callMethod('user.current')` при инициализации), независимо от роли (МКЦ или МП).

### Блоки формы (левая колонка)

| # | Блок | Поля |
|---|---|---|
| 1 | Финансовые данные | ФИО (auto), сумма долга, платёж, офиц. доход, неофиц. доход, зарплатная карта |
| 2 | Кредитная история | Кредиторы, залог, просрочки, ФССП, имущество, сделки |
| 3 | Личные данные | Место работы, семейное положение, дети, совм. имущество, судимости, ООО, ИП |
| 4 | Заметки менеджера | Исключение из КМ, основная боль, возражения, доп. комментарий |
| 5 | Запись к специалисту | Город клиента (timezone), МП, время клиента, время МП, ID события |

---

## Требования к расписанию (правая колонка)

### Календари МП

Календари **не привязаны к пользователю Bitrix24** — это отдельные календари-сущности с `OWNER_ID=0` и уникальным `CAL_TYPE` вида `MP[N]Vstrechi`. Занятость получается через `calendar.event.get` с `type=MP[N]Vstrechi, ownerId=0`, а не по `userId` и не через `calendar.accessibility.get`.

Рабочее время и таймзона каждого МП хранятся в `MP_WORK_DEFAULTS` (`anketa-kc/assets/mp-config.js`) — `calendar.section.get` для этих календарей не возвращает полей рабочего времени.

Реальные данные портала crm.yurclick.com (получены через `calendar.section.get?type=MP[N]Vstrechi&ownerId=0`):

| МП | CAL_TYPE | SECTION_ID | NAME (из Bitrix24) | Запись через БП 40 |
|---|---|---|---|---|
| МП 1 | `MP1Vstrechi` | 20 | Основные события | enumId=2099 |
| МП 2 | `MP2Vstrechi` | 15 | МП2 - Виталий Прилепин | enumId=2100 |
| МП 3 | `MP3Vstrechi` | 16 | МП3 - Полянский Феликс | enumId=2101 |
| МП 4 | `MP4Vstrechi` | 18 | МП4 - Ирина Рыжова | enumId=2102 |
| МП 5 | `MP5Vstrechi` | 19 | МП5 - Каролина Гнездилова | enumId=2103 |
| МП 6 | `MP6Vstrechi` | 74 | МП6 - Кондратьева Оксана | enumId=5086 |
| МП 7 | `MP7Vstrechi` | 80 | МП7 - Юлиана Сюткина | enumId=5092 |
| МП 8 | `MP8Vstrechi` | 81 | МП8 Филимонов Даниил | enumId=5101 |
| МП 9 | `MP9Vstrechi` | 82 | МП9 | enumId=null ⚠️ |
| МП 10 | `MP10Vstrechi` | 83 | МП10 | enumId=null ⚠️ |
| МП 11 | `MP11Vstrechi` | 84 | МП11 | enumId=null ⚠️ |

> **Примечание 1:** МП 9–11 пока не имеют enumId в `MP_BOOKING_ENUM_MAP` — бронировать встречу через БП 40 «Назначить встречу» для них нельзя. Календарь продолжает показывать их занятость, но кнопка «Назначить» будет неактивна. При назначении enum-значений в UF_CRM_*-поле сделки добавьте их в `MP_BOOKING_ENUM_MAP` (`mp-config.js`).
>
> **Примечание 2:** Рабочее время и UTC-смещение каждого МП хранятся в `MP_WORK_DEFAULTS` в `mp-config.js`. Если в Bitrix24 появятся поля рабочего времени в `calendar.section.get` — перенести их оттуда, убрав хардкод.

### Слоты

- Шаг слота — **60 минут** (только часовые слоты, получасовых нет).
- Рабочее время МП берётся из `MP_WORK_DEFAULTS` в `mp-config.js`.
- Занятые слоты определяются через `calendar.event.get` с `type=MP[N]Vstrechi, ownerId=0` для каждого из 11 календарей (11 параллельных вызовов).
- Событие считается «занятым», если `DELETED ≠ 'Y'` И `ACCESSIBILITY ≠ 'free'`.
- Границы события в UTC-мс вычисляются через `getEventUtcMs()` (из `tz-utils.js`) — он в первую очередь берёт `DATE_FROM_TS_UTC`/`DATE_TO_TS_UTC`, а при их отсутствии парсит `DATE_FROM`+`TZ_FROM` через `makeIsoWithTz()`.
- МП определяется по календарю-источнику, **а не по regex `/МП\s*\d+/` в `NAME`** — как это было в v2 через `_fetchMpMeetings` и `MEETINGS_OWNER_ID=137`.

### Логика часового пояса клиента

Приоритет — **часовой пояс клиента**.

**Шаг 1 — определение TZ клиента:**
- В блоке 5 МКЦ/МП выбирает город клиента из выпадающего списка (~30 городов России).
- Система определяет UTC-offset клиента из встроенного словаря (не внешний API).

**Шаг 2 — отображение слотов:**
- Каждый слот показывается в **двух колонках**:
  - `Время МП` — локальное время МП (его UTC)
  - `Время клиента` — конвертированное время в TZ клиента
- МКЦ ориентируется на колонку **«Время клиента»** при общении с клиентом.

**Шаг 3 — пересечение рабочего времени:**
- Отображаются только слоты, которые **одновременно** попадают в рабочее время МП и в «разумное» время клиента (09:00–20:00 по TZ клиента).
- Если свободных слотов сегодня < 3 — автоматически подгружается **следующий рабочий день** (пн–пт).
- Максимум автопереходов — **14 рабочих дней** вперёд. После этого показывается сообщение «Нет свободных слотов».
- Навигация `← Пред. день / След. день →` всегда видна для ручного переключения.

**Шаг 4 — визуальный пример:**
```
Клиент: Магадан (UTC+10)  |  МП: Самара (UTC+4)
Разница: клиент опережает МП на 6 часов

Рабочие часы МП: 09:00–17:00 (UTC+4) = 15:00–23:00 (UTC+10)
Разумное время клиента: 09:00–20:00 (UTC+10)
Пересечение: 15:00–20:00 (UTC+10) = 09:00–14:00 (UTC+4)

Доступные слоты: 09:00 / 10:00 / 11:00 / 12:00 / 13:00 / 14:00 МП
             = 15:00 / 16:00 / 17:00 / 18:00 / 19:00 / 20:00 клиент
```

---

## Требования к интеграции (Bitrix24)

| Операция | Метод |
|---|---|
| Получение данных лида | `crm.lead.get` |
| Получение текущего пользователя | `user.current` (отображение в шапке) |
| Получение списка пользователей | `user.get` (для сопоставления с МП) |
| Сохранение анкеты | `crm.lead.update` (22 × `UF_CRM_<ID>` полей, см. `assets/field-mapper.js`) |
| Запись в таймлайн | `crm.timeline.comment.add` |
| Получение занятых слотов | `calendar.accessibility.get` (по ID календаря МП) |
| Бронирование слота | `calendar.event.add` |
| Уведомление менеджера | `im.notify` |

---

## UF-поля лида (фактические коды портала)

Реальные UF-коды в Bitrix24 имеют формат `UF_CRM_<unix-timestamp>` (поля заведены
вручную, без запуска install.php). Источник истины — `assets/field-mapper.js`.

| Логическое имя | Реальный код UF | Тип | Блок |
|---|---|---|---|
| KC_CLIENT_CITY | `UF_CRM_1521214081` | string | Личные данные |
| KC_MARITAL_STATUS | `UF_CRM_1764767738244` | enumeration | Личные данные |
| KC_CHILDREN | `UF_CRM_1764767804861` | enumeration | Личные данные |
| KC_JOINT_PROPERTY | `UF_CRM_1764767768332` | enumeration | Личные данные |
| KC_CRIMINAL | `UF_CRM_1764767860124` | enumeration | Личные данные |
| KC_OOO | `UF_CRM_1764767873758` | enumeration | Личные данные |
| KC_IP | `UF_CRM_1764767897075` | enumeration | Личные данные |
| KC_DEBT_TOTAL | `UF_CRM_1764765055684` | string/integer | Финансовые данные |
| KC_SALARY_CARD | `UF_CRM_1764767677345` | enumeration | Финансовые данные |
| KC_CREDITORS | `UF_CRM_1764765826044` | string | Кредитная история |
| KC_OVERDUE | `UF_CRM_1764767202050` | string | Кредитная история |
| KC_FSSP | `UF_CRM_1764767243083` | string | Кредитная история |
| KC_KM_EXCLUSION | `UF_CRM_1764767905632` | string | Заметки менеджера |
| KC_MAIN_PAIN | `UF_CRM_1764767920445` | string | Заметки менеджера |
| KC_OBJECTIONS | `UF_CRM_1764767933689` | string | Заметки менеджера |
| KC_EXTRA_COMMENT | `UF_CRM_1764767947408` | string | Заметки менеджера |

Системные поля лида (пишутся напрямую): `LAST_NAME`, `NAME`, `SECOND_NAME`,
`TITLE`. Остальные логические поля из анкеты (FULLNAME, WORKPLACE,
MONTHLY_PAYMENT, INCOME_*, COLLATERAL, PROPERTY, DEALS, BOOKED_*) — либо
вычисляемые, либо хранятся локально в state, либо пишутся в событие календаря.

---

## Словарь городов (UTC-offset)

```js
const CITY_TZ = {
  'Москва': 3,         'Санкт-Петербург': 3, 'Новосибирск': 7,
  'Екатеринбург': 5,   'Казань': 3,          'Нижний Новгород': 3,
  'Красноярск': 7,     'Самара': 4,          'Уфа': 5,
  'Ростов-на-Дону': 3, 'Омск': 6,            'Краснодар': 3,
  'Воронеж': 3,        'Пермь': 5,           'Волгоград': 3,
  'Тюмень': 5,         'Иркутск': 8,         'Владивосток': 10,
  'Хабаровск': 10,     'Якутск': 9,          'Магадан': 10,
  'Чита': 9,           'Сочи': 3,            'Барнаул': 7,
  'Томск': 7,          'Оренбург': 5,        'Рязань': 3,
  'Ярославль': 3,      'Ижевск': 4,          'Севастополь': 3
};
```

---

## Совместимость

Tailwind CSS v4 требует: Safari 16.4+, Chrome 111+, Firefox 128+.

---

## Статус задач

| # | Задача | Статус |
|---|---|---|
| 1 | Зафиксировать стек в `README.md` и `tz.md` | ✅ Выполнено |
| 2 | Убрать `tokens.css` и `style.css` из `index.php` | ✅ Выполнено |
| 3 | Переключить `index.php` на Tailwind CDN + Flowbite | ✅ Выполнено |
| 4 | Рендер 26 полей через Tailwind/Flowbite в `form.js` | ✅ Выполнено |
| 5 | Рендер слотов расписания в `calendar.js` | ✅ Выполнено |
| 6 | Сбор данных формы / валидация / сохранение | ✅ Выполнено |
| 7 | Генерация рабочих слотов / пересечения / бронирование | ✅ Выполнено |
| 8 | Обновить `design-system.md` под Tailwind + Flowbite | ✅ Выполнено |
| 9 | Переименовать «Анкета КЦ» → «Анкета (МКЦ + МП)» | ✅ Выполнено |
| 10 | Шапка: текущий пользователь BX24 вместо роли | ✅ Зафиксировано в ТЗ |
| 11 | Слоты только часовые (шаг 60 мин) | ✅ Зафиксировано в ТЗ |
| 12 | Привязка расписания к ID календаря (не к userId) | ✅ Зафиксировано в ТЗ |
| 13 | Логика часового пояса клиента + двойное время в слотах | ✅ Зафиксировано в ТЗ |
| 14 | Автопереход на следующий день при нехватке слотов | ✅ Зафиксировано в ТЗ |
| 15 | Уточнить часы работы МП по скриншоту из Bitrix24 | ✅ Выполнено |
| 16 | Обновить CDN Tailwind: `cdn.tailwindcss.com` → `@tailwindcss/browser@4` | ✅ Выполнено |
| 17 | Очистить `style.css` и `tokens.css` (deprecated) | ✅ Выполнено |
| 18 | Timezone-логика в `calendar.js` (двойное время, защита от петли, актуализация TZ) | ✅ Выполнено |
| 19 | `BX24.callMethod('user.current')` в шапке `index.php` | ✅ Выполнено |
